# name: Deploy to GitHub Pages

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v3

#     - name: Setup Node.js
#       uses: actions/setup-node@v3
#       with:
#         node-version: '16'

#     - name: Install dependencies
#       run: npm install

#     - name: Build the project
#       run: npm run build

#     - name: Upload artifacts for deployment
#       uses: actions/upload-pages-artifact@v1
#       with:
#         path: ./dist  # This should match Vite's default output directory

#   deploy:
#     runs-on: ubuntu-latest
#     needs: build-and-deploy
#     permissions:
#       pages: write
#       id-token: write

#     steps:
#     - name: Deploy to GitHub Pages
#       uses: actions/deploy-pages@v1

name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Update this to your default branch if not 'main'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout the code
      - name: Checkout repository
        uses: actions/checkout@v3

      # Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16  # Ensure this matches your project setup

      # Install dependencies and build the project
      - name: Install dependencies and build
        run: |
          npm install
          npm run build

      # Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
